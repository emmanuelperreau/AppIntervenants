# AppIntervenants

## Description
Application web (PWA) de gestion d'intervenants.

## Stack
- HTML/CSS/JS vanilla + TypeScript (via Vite)
- PWA avec Service Worker
- Pas de framework frontend

## Commandes
- `npm install` : installer les dépendances
- `npx vite build` : build de production
- `npx vite` : serveur de développement

## Structure
Voir synthese.md pour l'arborescence complète.
